import { breakpoints } from './breakpoints';
import { buttons } from './buttons';
import { colors } from './colors';
import { size, layout, alignChildren, flexDir, flexGrow } from './layout';
import { spacing } from './spacing';

export const theme = {
  colors,
  size,
  spacing,
  layout,
  flexDir,
  flexGrow,
  alignChildren,
  breakpoints,
  buttons,
};
